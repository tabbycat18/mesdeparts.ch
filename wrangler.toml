name = "mesdeparts-ch"
main = "cloudflare-worker/worker.js"
compatibility_date = "2025-12-17"

# Uncomment and fill your zone_id to bind a custom domain via CLI (optional)
# routes = [
#   { pattern = "api.mesdeparts.ch/*", zone_id = "YOUR_ZONE_ID" }
# ]
